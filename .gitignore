# --- DÉPENDANCES ---
# On ne versionne jamais les dossiers node_modules (trop lourds)
node_modules/
backend/node_modules/
frontend/node_modules/

# --- BASE DE DONNÉES (SQLite & Prisma) ---
# On ignore la base de données active pour éviter les conflits entre membres
# Chacun doit générer la sienne ou utiliser le script de seed
backend/prisma/*.db
backend/prisma/*.db-journal
# Si tu veux quand même partager ta DB de test, commente la ligne ci-dessus avec #

# --- VARIABLES D'ENVIRONNEMENT ---
# Contient des secrets ou des configurations locales (ports, clés API)
.env
backend/.env
frontend/.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- BUILDS & PRODUCTIONS ---
# Fichiers générés lors de la compilation
frontend/dist/
frontend/build/
dist_electron/
out/
build/

# --- LOGS ---
# Historiques d'erreurs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- SYSTÈME & IDE ---
# Fichiers spécifiques aux systèmes d'exploitation ou éditeurs
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# --- TEST ---
coverage/
