{
  "name": "logiciel-etudiants",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
     "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
     "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run open-app\"",
  "open-app": "node -e \"const exec = require('child_process').exec; const url = 'http://localhost:5173'; const cmd = process.platform === 'win32' ? 'start chrome --app=' + url : 'google-chrome --app=' + url + ' --new-window'; setTimeout(() => exec(cmd), 3000)\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "open-cli": "^8.0.0"
  }
}
